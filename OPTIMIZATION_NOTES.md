# 最適化内容 v8

見た目と演出を大きく変えず、特に「大量に重なって安定した山」で残りやすい **グループ再探索コスト** をさらに削るため、`game.js` に **凍結クラスタキャッシュ + 前線だけ局所再探索するハイブリッド方式** を追加しました。

## 今回の新アルゴリズム
- **settled cluster cache**
  - 凍結済み・静止済みの同種アイテム群を、`settled touch graph` から **クラスタ単位でキャッシュ**
  - 山が安定している間は、毎回 spatial hash から全体近傍探索し直さず、**既知の凍結クラスタを再利用**
- **frontier local rescan**
  - 動いている / 直近に落ちた / dirty / 高精度物理中 / 表層に近いアイテムだけを **frontier seed** として抽出
  - その seed 周辺だけ `buildTouchGroupsNearSeeds` で局所再探索
- **hybrid merge**
  - `frontier` で拾った動的グループと、`settled cluster cache` の静的グループを **重複なしでマージ**
  - これにより、巨大な安定山はキャッシュで済ませつつ、上側の変化だけ正確に追従

## 効く場面
- アイテム数が多く、下層がほぼ凍結済みなのに、上側だけ少しずつ動く場面
- 同じ山を scan のたびに毎回ほぼ全探索していた場面
- current / next trend のプレビュー更新で、密集時に再探索コストが膨らむ場面

## 主な実装変更
- **凍結済みクラスタのキャッシュ層**を追加
- **frontier seed 抽出**を追加
- `buildTouchGroupsForIndex()` と `buildTouchGroupsForType()` を、
  **フル spatial hash 探索 → ハイブリッド探索優先** に変更
- 安定山では **凍結クラスタをそのまま再利用** し、
  動いている前線だけを近傍再探索するよう変更

## 狙い
- 大量密集時に、下層の安定山まで毎回再探索しない
- 上側の見た目・演出・判定精度はできるだけ維持しつつ、
  **重い時だけ処理量のオーダーをもう一段落とす**
- 特にスマホや低めの CPU で、密集終盤のカクつきを減らす

## 変更ファイル
- `game.js`
- `OPTIMIZATION_NOTES.md`
